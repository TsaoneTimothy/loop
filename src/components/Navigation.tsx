// This file is kept as a placeholder but the component is no longer used.
// We've replaced it with AppSidebar.tsx
// The file is kept to maintain project structure.

import { NavLink } from "react-router-dom";
import { Home, MessageCircle, User, Store, Plus } from "lucide-react";

const Navigation = () => {
  return null; // Component no longer used
};

export default Navigation;
